import { theme } from '../../theme';

export const config = {
    defaultIconColor: theme.palette.greyDarkColor,
    minPortFontSize: 20,
    maxPortFontSize: 50,
    minPortWidth: 45,
    rowLimit: 10,
    borderRight: `1px solid ${theme.palette.greyLightColor}`,
}
