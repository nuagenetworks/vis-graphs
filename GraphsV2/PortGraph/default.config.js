import { theme } from '../../theme';

export const config = {
    defaultIconColor: theme.palette.greyDarkColor,
    minPortWidth: 45,
    rowLimit: 16,
    borderRight: `1px solid ${theme.palette.greyLightColor}`,
    background: '#f2f2f2',
    removeUpperColumnName: false,
    removeLowerColumnName: false,
    portIconSize: 18,
}
