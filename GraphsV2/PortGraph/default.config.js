import { theme } from '../../theme';

export const config = {
    defaultIconColor: theme.palette.greyDarkColor,
    minPortWidth: 45,
    rowLimit: 16,
    background: '#f2f2f2',
    showUpperColumnName: true,
    showLowerColumnName: true,
    minPortFontSize: 20,
    maxPortFontSize: 50,
}
