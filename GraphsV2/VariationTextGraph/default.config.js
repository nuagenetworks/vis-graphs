import { theme } from "../../theme";

export const config = {
    fontSize: "1.5em",
    fontWeight: 500,
    drawColors: theme.palette.yellowDarkColor,
    negativeColors: theme.palette.redDarkColor,
    positiveColors: theme.palette.greenDarkColor,
    showVariation: true
}
