import { theme } from '../../theme';

export const properties = {
    defaultIconColor: theme.palette.greyDarkColor,
    minPortFontSize: 20,
    maxPortFontSize: 50,
    minPortWidth: 45,
    rowLimit: 10,
}
