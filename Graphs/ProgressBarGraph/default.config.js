import { theme } from '../../theme';

export const config = {
    barColor: theme.palette.blueColor,
    backgroundColor: theme.palette.greyLightColor,
    minSectionHeight: 30,
    maxSectionHeight: 50,
    defaultRange: 100,
    fontSize:'10px',
}
