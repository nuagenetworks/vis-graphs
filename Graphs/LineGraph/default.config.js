import { theme } from "../../theme"

export const properties = {
    stroke: {
        color: "red",
        width: "2px"
    },
    legend: {
        show: false
    },
    zeroStart: true,
    circleRadius: 5,
    defaultYColor: theme.palette.greenColor,
    chartWidthToPixel: 7,
}
